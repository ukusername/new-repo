# 📖 Подробное описание проекта: Telegram-бот для регистрации на события

## 🎯 Общее назначение проекта

Это полнофункциональный Telegram-бот, разработанный на Python, который позволяет пользователям просматривать предстоящие события, регистрироваться на них и получать автоматические напоминания. Проект построен на современном технологическом стеке с использованием асинхронного программирования, базы данных PostgreSQL и архитектуры, готовой к масштабированию.

---

## 🏗️ Архитектура системы

### Технологический стек

**Основные технологии:**
- **Python 3.11+** - основной язык программирования
- **FastAPI** - веб-фреймворк для обработки webhook-запросов от Telegram
- **aiogram 3.22+** - современная библиотека для работы с Telegram Bot API
- **PostgreSQL** - реляционная база данных для хранения всех данных
- **SQLAlchemy 2.0** - ORM для работы с базой данных
- **APScheduler** - планировщик задач для автоматических напоминаний
- **Pydantic** - валидация данных и управление настройками
- **Loguru** - продвинутое логирование

**Архитектурный паттерн:**
Проект следует принципам **многослойной архитектуры** с четким разделением ответственности:
- **Слой представления** (Telegram handlers) - обработка пользовательских взаимодействий
- **Слой бизнес-логики** (Services) - реализация бизнес-правил
- **Слой доступа к данным** (Repository) - абстракция работы с БД
- **Слой данных** (Models) - модели данных и схемы БД

---

## 📁 Структура проекта и назначение модулей

### 1. **main.py** - Точка входа приложения

Это центральный файл, который инициализирует и запускает всё приложение.

**Ключевые функции:**
- Создает экземпляры бота (Bot) и диспетчера (Dispatcher) из aiogram
- Настраивает FastAPI приложение с lifecycle-менеджментом
- Инициализирует базу данных при старте
- Автоматически настраивает Telegram webhook (или использует домен Replit)
- Запускает планировщик задач для напоминаний
- Предоставляет HTTP endpoints:
  - `POST /telegram` - webhook для получения обновлений от Telegram
  - `GET /health` - проверка работоспособности системы
  - `GET /` - информация о приложении

**Жизненный цикл приложения:**
1. При старте: инициализация БД → настройка webhook → запуск планировщика
2. Во время работы: обработка webhook-запросов → выполнение задач по расписанию
3. При остановке: остановка планировщика → удаление webhook → закрытие сессий

### 2. **config.py** - Управление конфигурацией

Использует Pydantic Settings для управления всеми настройками приложения через переменные окружения.

**Настройки:**
- `BOT_TOKEN` - токен Telegram бота (обязательный)
- `WEBHOOK_URL` - URL для webhook (опционально, может быть автодетектирован)
- `WEBHOOK_PATH` - путь для webhook (по умолчанию `/telegram`)
- `DATABASE_URL` - строка подключения к PostgreSQL
- `HOST` и `PORT` - настройки веб-сервера
- `LOG_LEVEL` - уровень логирования

### 3. **database/** - Работа с базой данных

#### **models.py** - Модели данных (SQLAlchemy ORM)

Определяет 4 основные таблицы:

**1. User (Пользователи)**
- Хранит информацию о пользователях системы
- Поля: `id`, `name`, `email` (уникальный), `telegram_id` (уникальный, опционально), `whatsapp_phone` (уникальный, опционально)
- Поддерживает связывание аккаунтов по email (готовность к интеграции с WhatsApp)
- Автоматические временные метки: `created_at`, `updated_at`
- Связь один-ко-многим с регистрациями

**2. Event (События)**
- Хранит информацию о событиях
- Поля: `id`, `title`, `description`, `date_time`, `location`, `max_participants`
- Индексированное поле `date_time` для быстрого поиска по времени
- Связь один-ко-многим с регистрациями

**3. EventRegistration (Регистрации)**
- Связывает пользователей и события
- Поля: `id`, `user_id` (FK), `event_id` (FK), `registered_via` (платформа), `status` (confirmed/cancelled)
- Отслеживает, через какую платформу была сделана регистрация
- Поддерживает отмену регистраций (статус cancelled)

**4. UserState (Состояния FSM)**
- Хранит состояния конечного автомата (FSM) для каждого пользователя
- Позволяет сохранять состояние между перезапусками приложения
- Поля: `user_platform_id`, `platform`, `current_state`, `state_data` (JSON)
- Используется для многошаговых процессов (например, регистрация)

**Enum-типы:**
- `RegistrationStatus` - статусы регистрации (confirmed, cancelled)
- `Platform` - платформы (telegram, whatsapp) - готовность к расширению

#### **connection.py** - Подключение к БД

- Создает engine SQLAlchemy с пулом соединений (pool_size=10, max_overflow=20)
- Настраивает sessionmaker для создания сессий
- Функция `init_db()` - создает все таблицы при первом запуске
- Контекстный менеджер `get_db()` - автоматически управляет транзакциями:
  - Открывает сессию
  - При успехе - коммитит изменения
  - При ошибке - откатывает транзакцию
  - Всегда закрывает сессию

#### **repository.py** - Репозитории (паттерн Repository)

Абстракция над SQLAlchemy, предоставляющая удобные методы для работы с данными.

**UserRepository:**
- `get_by_telegram_id()` - поиск пользователя по Telegram ID
- `get_by_email()` - поиск пользователя по email
- `create()` - создание нового пользователя
- `update_telegram_id()` - привязка Telegram ID к существующему пользователю

**EventRepository:**
- `get_all()` - все события
- `get_upcoming()` - только предстоящие события (дата > сейчас)
- `get_by_id()` - событие по ID
- `create()` - создание события
- `get_events_in_timeframe()` - события в определенном временном окне (для напоминаний)

**RegistrationRepository:**
- `get_by_user_and_event()` - проверка существующей регистрации
- `create()` - создание регистрации
- `get_confirmed_by_event()` - все подтвержденные регистрации на событие
- `get_user_registrations()` - все регистрации пользователя
- `cancel()` - отмена регистрации

**StateRepository:**
- `get_state()` - получение состояния FSM пользователя
- `set_state()` - установка/обновление состояния
- `clear_state()` - очистка состояния

### 4. **telegram/** - Обработка Telegram взаимодействий

#### **handlers.py** - Обработчики команд и сообщений

Основной файл, обрабатывающий все взаимодействия пользователей с ботом.

**Команды:**
- `/start` - приветствие и главное меню с кнопками
- `/help` - справка по всем командам
- `/events` - список всех предстоящих событий
- `/register` - начало процесса регистрации
- `/my_registrations` - список регистраций пользователя

**Обработка callback-запросов (кнопки):**
- `menu:*` - навигация по главному меню
- `event:*` - выбор события для регистрации
- `confirm:*` - подтверждение/отмена регистрации

**Обработка текстовых сообщений:**
- В зависимости от текущего состояния FSM обрабатывает ввод имени или email
- Валидирует введенные данные
- Переводит пользователя между состояниями регистрации

**Процесс регистрации (FSM):**
1. Пользователь выбирает событие → состояние `CHOOSING_EVENT`
2. Вводит имя → валидация → состояние `ENTERING_NAME`
3. Вводит email → валидация → состояние `ENTERING_EMAIL`
4. Подтверждает данные → состояние `CONFIRMING`
5. Создается регистрация → состояние очищается

**Обработка ошибок:**
- Проверка доступности мест на событии
- Проверка дублирования регистраций
- Валидация email и имени
- Обработка исключений с логированием

#### **states.py** - Состояния FSM

Определяет enum `RegistrationState` с состояниями процесса регистрации:
- `CHOOSING_EVENT` - выбор события
- `ENTERING_NAME` - ввод имени
- `ENTERING_EMAIL` - ввод email
- `CONFIRMING` - подтверждение данных

#### **keyboards.py** - Клавиатуры для бота

Создает интерактивные кнопки (InlineKeyboard) для удобной навигации:

**get_main_menu_keyboard()** - главное меню:
- "📋 Доступные события"
- "📝 Мои регистрации"
- "ℹ️ Помощь"

**get_events_keyboard(events)** - список событий:
- Каждое событие - отдельная кнопка
- Показывает количество свободных мест
- Callback data содержит ID события

**get_confirmation_keyboard()** - подтверждение регистрации:
- "✅ Подтвердить"
- "❌ Отменить"

### 5. **services/** - Бизнес-логика приложения

#### **user_service.py** - Сервис пользователей

**get_or_create_telegram_user():**
- Умная логика создания/получения пользователя
- Если пользователь с таким Telegram ID существует - возвращает его
- Если нет, но есть пользователь с таким email:
  - Если у него нет Telegram ID - привязывает текущий
  - Если уже привязан другой Telegram ID - ошибка
- Если пользователя нет вообще - создает нового
- Это позволяет связывать аккаунты по email (готовность к WhatsApp)

**get_user_by_telegram_id()** - простой поиск пользователя

#### **event_service.py** - Сервис событий

- `get_upcoming_events()` - получение предстоящих событий
- `get_event_by_id()` - получение события по ID
- `get_events_for_reminder()` - события для отправки напоминаний (временное окно)

#### **registration_service.py** - Сервис регистраций

**register_user_for_event():**
- Проверяет, не зарегистрирован ли пользователь уже
- Проверяет наличие свободных мест
- Создает регистрацию
- Логирует действия

**get_user_registrations()** - регистрации пользователя
**get_event_participants()** - участники события

#### **fsm_service.py** - Сервис управления состояниями

Управляет состояниями конечного автомата (FSM) для многошаговых процессов.

**Функции:**
- `get_state()` - получение текущего состояния
- `get_state_data()` - получение данных состояния (JSON)
- `set_state()` - установка состояния с данными
- `clear_state()` - очистка состояния
- `update_state_data()` - обновление данных состояния

**Особенности:**
- Состояния сохраняются в БД (персистентность между перезапусками)
- Данные состояния хранятся в JSON формате
- Поддержка разных платформ (готовность к WhatsApp)

#### **notification_service.py** - Сервис уведомлений

**send_event_reminders():**
- Отправляет напоминания о событии всем зарегистрированным пользователям
- Форматирует сообщение через `format_reminder()`
- Отправляет только пользователям с Telegram ID
- Обрабатывает ошибки отправки (логирует, но не прерывает процесс)

### 6. **scheduler/** - Планировщик задач

#### **scheduler.py** - Настройка планировщика

Использует APScheduler (AsyncIOScheduler) для периодических задач.

**Настроенные задачи:**
1. **reminder_24h** - проверка событий за 24 часа до начала
   - Запускается каждый час
   - Ищет события, которые начнутся через 24 часа (±30 минут)
   - Отправляет напоминания всем зарегистрированным

2. **reminder_1h** - проверка событий за 1 час до начала
   - Запускается каждый час
   - Ищет события, которые начнутся через 1 час (±30 минут)
   - Отправляет напоминания

**Особенности:**
- Асинхронный планировщик (не блокирует основной поток)
- Толерантность ±30 минут для учета времени выполнения задач
- Задачи заменяются при перезапуске (`replace_existing=True`)

#### **tasks.py** - Задачи планировщика

**check_and_send_reminders():**
- Получает список событий в нужном временном окне
- Для каждого события получает список регистраций
- Отправляет напоминания через NotificationService
- Логирует все действия и ошибки

### 7. **utils/** - Вспомогательные утилиты

#### **validators.py** - Валидация данных

**validate_email_address():**
- Использует библиотеку `email-validator`
- Проверяет формат email
- Нормализует email (приводит к стандартному виду)
- Возвращает кортеж (успех, результат/ошибка)

**validate_name():**
- Проверяет длину имени (минимум 2, максимум 100 символов)
- Удаляет пробелы по краям
- Возвращает кортеж (успех, валидное имя/ошибка)

#### **formatters.py** - Форматирование сообщений

Функции для красивого отображения данных в Telegram:

- `format_event()` - форматирование одного события
- `format_registration()` - сообщение о успешной регистрации
- `format_event_list()` - список событий
- `format_reminder()` - напоминание о событии

Все используют HTML-разметку для красивого отображения (жирный текст, эмодзи).

### 8. **Вспомогательные скрипты**

#### **setup_webhook.py** - Настройка webhook

Отдельный скрипт для ручной настройки webhook:
- Получает токен бота и домен из переменных окружения
- Устанавливает webhook на указанный URL
- Показывает информацию о текущем webhook
- Полезен для отладки и первоначальной настройки

#### **add_sample_events.py** - Добавление тестовых событий

Скрипт для наполнения базы данных тестовыми событиями:
- Создает 4 примера событий с разными датами
- Одно событие через 25 часов (для тестирования напоминаний)
- Остальные через 7, 14 и 30 дней
- Разные локации и количество мест

---

## 🔄 Потоки данных и взаимодействия

### 1. Процесс регистрации пользователя

```
Пользователь → /register
    ↓
Бот показывает список событий (кнопки)
    ↓
Пользователь выбирает событие
    ↓
FSM: состояние CHOOSING_EVENT → ENTERING_NAME
    ↓
Пользователь вводит имя
    ↓
Валидация имени → FSM: ENTERING_EMAIL
    ↓
Пользователь вводит email
    ↓
Валидация email → FSM: CONFIRMING
    ↓
Бот показывает данные для подтверждения (кнопки)
    ↓
Пользователь подтверждает
    ↓
UserService.get_or_create_telegram_user() - создание/получение пользователя
    ↓
RegistrationService.register_user_for_event() - создание регистрации
    ↓
Проверки: дубликат? места есть?
    ↓
Сохранение в БД → FSM очищается
    ↓
Бот отправляет подтверждение регистрации
```

### 2. Процесс отправки напоминаний

```
APScheduler (каждый час)
    ↓
check_and_send_reminders(24 часа)
    ↓
EventService.get_events_for_reminder() - поиск событий
    ↓
Для каждого события:
    ↓
RegistrationService.get_event_participants() - получение регистраций
    ↓
NotificationService.send_event_reminders() - отправка
    ↓
Для каждого пользователя:
    ↓
bot.send_message() - отправка в Telegram
    ↓
Логирование результата
```

### 3. Обработка webhook-запросов

```
Telegram → POST /telegram (webhook)
    ↓
FastAPI получает JSON с обновлением
    ↓
dp.feed_update() - передача в aiogram
    ↓
Dispatcher находит подходящий handler
    ↓
Handler обрабатывает команду/сообщение/callback
    ↓
Обращение к сервисам → работа с БД
    ↓
Формирование ответа → отправка пользователю
```

---

## 🗄️ Схема базы данных

### Таблица `users`
```
id (PK, Integer)
name (String, NOT NULL)
email (String, UNIQUE, NOT NULL, INDEXED)
telegram_id (BigInteger, UNIQUE, NULLABLE, INDEXED)
whatsapp_phone (String, UNIQUE, NULLABLE, INDEXED)
created_at (DateTime)
updated_at (DateTime)
```

### Таблица `events`
```
id (PK, Integer)
title (String, NOT NULL)
description (String, NOT NULL)
date_time (DateTime, NOT NULL, INDEXED)
location (String, NOT NULL)
max_participants (Integer, NOT NULL, default=50)
created_at (DateTime)
```

### Таблица `event_registrations`
```
id (PK, Integer)
user_id (FK → users.id, NOT NULL)
event_id (FK → events.id, NOT NULL)
registered_via (String, NOT NULL)  # "telegram" или "whatsapp"
status (String, NOT NULL, default="confirmed")  # "confirmed" или "cancelled"
created_at (DateTime)
```

### Таблица `user_states`
```
id (PK, Integer)
user_platform_id (String, UNIQUE, NOT NULL, INDEXED)  # "telegram:123456"
platform (String, NOT NULL)  # "telegram" или "whatsapp"
current_state (String, NULLABLE)  # состояние FSM
state_data (String, NULLABLE)  # JSON с данными состояния
updated_at (DateTime)
```

**Связи:**
- User 1:N EventRegistration
- Event 1:N EventRegistration
- UserState - независимая таблица для FSM

---

## 🔐 Безопасность и валидация

### Валидация входных данных

1. **Email:**
   - Проверка формата через `email-validator`
   - Нормализация (приведение к стандартному виду)
   - Защита от SQL-инъекций через ORM

2. **Имя:**
   - Минимальная длина: 2 символа
   - Максимальная длина: 100 символов
   - Удаление пробелов по краям

3. **Регистрации:**
   - Проверка дубликатов (нельзя зарегистрироваться дважды)
   - Проверка доступности мест
   - Проверка существования события

### Управление транзакциями

- Все операции с БД обернуты в транзакции
- Автоматический rollback при ошибках
- Изоляция операций через контекстные менеджеры

---

## 📊 Логирование

Проект использует **Loguru** для продвинутого логирования:

- Настройка уровня логирования через конфигурацию
- Логирование всех важных действий:
  - Регистрации пользователей
  - Создание регистраций
  - Отправка напоминаний
  - Ошибки и исключения
  - Работа планировщика
  - Изменения состояний FSM

---

## 🚀 Развертывание и запуск

### Требования

1. **Переменные окружения:**
   - `BOT_TOKEN` - токен Telegram бота
   - `DATABASE_URL` - строка подключения к PostgreSQL
   - `WEBHOOK_URL` (опционально) - URL для webhook
   - `REPLIT_DOMAINS` (опционально) - для автодетекта домена

2. **База данных:**
   - PostgreSQL должна быть доступна
   - Таблицы создаются автоматически при первом запуске

3. **Зависимости:**
   - Все зависимости указаны в `pyproject.toml`
   - Установка через `uv` или `pip`

### Процесс запуска

1. Установка зависимостей
2. Настройка переменных окружения
3. Запуск `main.py` (или через uvicorn)
4. Автоматическая инициализация БД
5. Автоматическая настройка webhook
6. Запуск планировщика
7. Готово к работе!

### Первоначальная настройка

1. Запустить `add_sample_events.py` для добавления тестовых событий
2. (Опционально) Запустить `setup_webhook.py` для проверки webhook

---

## 🔮 Готовность к расширению

Проект спроектирован с учетом будущих расширений:

1. **Интеграция с WhatsApp:**
   - Модель User уже содержит поле `whatsapp_phone`
   - Enum Platform содержит "whatsapp"
   - FSM поддерживает разные платформы
   - Связывание аккаунтов по email уже реализовано

2. **Дополнительные функции:**
   - Отмена регистраций (статус cancelled уже есть)
   - Email-уведомления (структура готова)
   - Админ-панель (можно добавить без изменения архитектуры)
   - Мультиязычность (можно добавить через конфигурацию)

---

## 📈 Масштабируемость

### Текущие ограничения и решения

1. **Пул соединений БД:**
   - Настроен пул из 10 соединений с overflow до 20
   - Можно увеличить при необходимости

2. **Планировщик:**
   - Запускается в одном процессе
   - Для масштабирования можно вынести в отдельный сервис

3. **Webhook:**
   - Обрабатывается синхронно
   - FastAPI поддерживает асинхронную обработку (уже используется)

### Рекомендации для масштабирования

- Использовать Redis для кеширования
- Вынести планировщик в отдельный микросервис
- Добавить очереди для отправки уведомлений
- Использовать connection pooling на уровне приложения

---

## 🎨 Особенности реализации

### 1. Персистентный FSM

В отличие от стандартного FSM в aiogram, который хранится в памяти, здесь состояния сохраняются в БД. Это означает:
- Состояния сохраняются между перезапусками приложения
- Можно восстановить процесс регистрации после сбоя
- Поддержка нескольких инстансов приложения

### 2. Умное связывание аккаунтов

Система автоматически связывает аккаунты по email:
- Если пользователь регистрируется с email, который уже есть в системе, но с другим Telegram ID, система привязывает новый Telegram ID
- Это позволяет использовать один аккаунт на разных платформах

### 3. Гибкая система напоминаний

- Напоминания отправляются в окне ±30 минут от целевого времени
- Это учитывает время выполнения задач планировщика
- Можно легко добавить дополнительные временные точки (например, за неделю)

### 4. Чистая архитектура

- Разделение на слои (handlers → services → repositories → models)
- Легко тестировать каждый слой отдельно
- Легко добавлять новые функции
- Следование принципам SOLID

---

## 🐛 Обработка ошибок

### Уровни обработки ошибок

1. **Валидация данных:**
   - Пользователю показывается понятное сообщение об ошибке
   - Процесс не прерывается, можно исправить

2. **Бизнес-логика:**
   - Проверка доступности мест
   - Проверка дубликатов
   - Понятные сообщения об ошибках

3. **Технические ошибки:**
   - Логирование всех исключений
   - Откат транзакций при ошибках БД
   - Graceful degradation (система продолжает работать при частичных сбоях)

4. **Отправка уведомлений:**
   - Ошибки при отправке не прерывают процесс
   - Логируются для последующего анализа
   - Можно реализовать retry-механизм

---

## 📝 Заключение

Этот проект представляет собой **профессионально разработанный Telegram-бот** с:
- ✅ Чистой архитектурой
- ✅ Полноценной работой с БД
- ✅ Автоматическими напоминаниями
- ✅ Удобным пользовательским интерфейсом
- ✅ Готовностью к масштабированию
- ✅ Готовностью к расширению функционала

Проект демонстрирует использование современных практик разработки на Python и может служить основой для более сложных систем регистрации и управления событиями.


